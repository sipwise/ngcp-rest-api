export enum ContractType {
    SipPeering = 'sippeering',
    Reseller = 'reseller'
}

// export type ContractStatus = 'active' | 'pending' | 'locked' | 'terminated'

export enum ContractStatus {
    Active = 'active',
    Pending = 'pending',
    Locked = 'locked',
    Terminated = 'terminated'
}
