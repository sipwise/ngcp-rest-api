#!/usr/bin/make -f

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE = 1

YARN_BIN := ".yarn/releases/yarn-4.17.1.cjs"

%:
	dh $@

override_dh_auto_install:
	$(YARN_BIN) cache clean
	$(YARN_BIN) install
	$(YARN_BIN) test
	$(YARN_BIN) build:prod
